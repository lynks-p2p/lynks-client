
function createID() {
  return 0;
}

function saveHost() {
  // store (shred, host) in DHT
  return 0;
}

function retrieveHost() {
  // load (shred, host) from DHT

  return 0;
}

export { createID, saveHost, retrieveHost };
